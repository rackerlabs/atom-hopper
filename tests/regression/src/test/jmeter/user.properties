#---------------------------------------------------------------------------
# AtomHopper Tests Configuration
#---------------------------------------------------------------------------

# HTTP requests will typically target 
#   http://${host}:${port}/${path}/${namespace}/${feed}
# If 'path' is emtpy, then they will target 
#   http://${host}:${port}/${namespace}/${feed}

host=108.166.96.153
#host=atom.staging.ord1.us.ci.rackspace.net
path=
port=8080
namespace=namespace
feed=feed

buildinfo.version=1.0.1-SNAPSHOT
buildinfo.groupid=org.atomhopper
buildinfo.artifactid=atomhopper
