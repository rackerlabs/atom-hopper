<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/hopper.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- Root Logger -->
    <root level="debug">
        <appender-ref ref="FILE" />
    </root>
    <!-- Logger for your class/package -->
    <logger name="org.atomhopper.util.jsonparsingwork" level="debug" additivity="false">
        <appender-ref ref="FILE" />
    </logger>
</configuration>
